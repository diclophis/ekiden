source 'https://rubygems.org'

ruby '2.2.3'

# Rack provides a minimal, modular and adaptable interface for developing web applications in Ruby
gem 'rack'

# Rake is a Make-like program implemented in Ruby.
gem 'rake'

# A ruby web server built for concurrency http://puma.io
gem 'puma'

# Easily create and manage an APT repository on S3
gem 'deb-s3'

# Effing package management! Build packages for multiple platforms (deb, rpm, etc) with great ease and sanity.
gem 'fpm'

# cloud services library
gem 'fog'

# The mime-types library provides a library and registry for information about MIME content type definitions.
gem 'mime-types'

# markup as ruby (official repository)
gem 'markaby', git: 'https://github.com/mavenlink/markaby.git', branch: 'fix-ruby-2.2-warning'

# Resque is a Redis-backed Ruby library for creating background jobs, placing them on multiple queues, and processing them later. http://resque.github.io/
gem 'resque'

# A resque plugin that ensures that only one job for a given queue will be running on any worker at a given time.
gem 'resque-lonely_job'
